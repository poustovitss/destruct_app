To increase perfomance of application in future, I decided to set nil value to message instead of deleting it.
In future we can implement worker to delete messages which links expired month ago, etc.
